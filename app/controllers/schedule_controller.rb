class ScheduleController < ApplicationController
    before_action :authenticate_user!
    #Add a time slot to weekly availability
    def add_time_slot
        
    end
    #Remove a time slot from weekly availability
    def remove_time_slot

    end
end
