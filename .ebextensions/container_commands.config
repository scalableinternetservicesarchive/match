container_commands:
  seeddb:
    command: 'export HOME=/root; RAILS_ENV=production rake db:migrate; rake db:seed; mkdir /home/webapp; chown webapp:webapp /home/webapp; chmod 700 /home/webapp'
    leader_only: true
