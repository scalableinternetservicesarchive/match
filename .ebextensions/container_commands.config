container_commands:
  seeddb:
    command: 'export HOME=/root; RAILS_ENV=production rake db:migrate; rake db:seed; sudo mkdir /home/webapp; sudo chown webapp:webapp /home/webapp; sudo chmod 700 /home/webapp'
    leader_only: true
